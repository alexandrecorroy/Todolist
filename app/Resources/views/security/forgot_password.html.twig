{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        {{ form_start(form, {'action' : path('forgot_password')}) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-success pull-right">Générer un nouveau mot de passe</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
